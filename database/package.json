{
  "name": "@accountability-coach/database",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma schema + migrations (PostgreSQL)",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
