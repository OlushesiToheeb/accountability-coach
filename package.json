{
  "name": "accountability-coach",
  "version": "0.0.1",
  "private": true,
  "description": "AI accountability & habit coach — monorepo (backend, app, database)",
  "workspaces": [
    "backend",
    "app",
    "database"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace backend",
    "dev:app": "npm run start --workspace app",
    "db:generate": "npm run generate --workspace database",
    "db:migrate": "npm run migrate --workspace database"
  },
  "license": "UNLICENSED"
}
